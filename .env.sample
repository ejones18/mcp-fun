# =============================================================================
# Environment Variables for MCP Server
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to source control!

# Azure ML Managed Online Endpoint Configuration
# Get these from your Azure ML workspace > Endpoints > Your endpoint > Consume tab
AML_SCORE_URL=https://your-workspace.region.inference.ml.azure.com/score
AML_API_KEY=your-api-key-here
AML_DEFAULT_DEPLOYMENT=your-deployment-name

# Server Configuration
# Azure Container Apps will override PORT automatically
PORT=8080
